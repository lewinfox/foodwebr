% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/function-matrix.R
\name{functions_passed_by}
\alias{functions_passed_by}
\title{Which functions are passed to higher-order functions?}
\usage{
functions_passed_by(f)
}
\arguments{
\item{f}{\verb{<fn>} The function to analyse.}
}
\value{
A character vector of function names.
}
\description{
\code{codetools::findGlobals()} only reports functions that are called directly. A function passed
by name, as in \code{do.call("foo", args)} or \code{lapply(x, foo)}, shows up as a string or a variable
and is missed. This walks the body (and default arguments) of \code{f} looking for calls to the
functions in \code{higher_order_functions} and returns the names of any functions passed to them as
a bare name or a string. For purrr-style formula lambdas such as \code{map(x, ~ foo(.x))}, the
functions called inside the formula are returned.
}
\details{
Names that are local to \code{f}, or to an anonymous function inside it, are ignored, as they refer
to the local object rather than a function in the environment.
}
\keyword{internal}
